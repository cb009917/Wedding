# Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# .env is in .gitignore and will not be committed to version control

# Google Maps API Key (for DirectionsSection)
# Required for embedding Google Maps on the directions page
# Get your key at: https://console.cloud.google.com/
# Enable Maps Embed API for your project
# Restrict the key to your domain (HTTP referrers) for security
# Documentation: https://developers.google.com/maps/documentation/embed/get-api-key
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Google Sheets API Key (for TableLookup)
# Required for fetching guest table assignments from Google Sheets
# Can be the same key as Maps API or a separate one
# Enable Google Sheets API for your project
# Restrict the key to your domain (HTTP referrers) for security
# Documentation: https://developers.google.com/sheets/api/guides/authorizing
VITE_GOOGLE_SHEETS_API_KEY=your_google_sheets_api_key_here

# Google Sheets Spreadsheet ID (optional)
# Can also be configured in src/data/weddingData.js
# Get from your Google Sheets URL: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
# The spreadsheet must be shared as "Anyone with the link can view"
VITE_GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here

# Security Notes:
# - These API keys are exposed in client-side code
# - Always restrict keys to your specific domain(s)
# - Never use keys with write permissions
# - For production, use HTTP referrer restrictions
# - For development, you can use localhost restrictions
# - The Google Sheet should be read-only (Viewer access only)
